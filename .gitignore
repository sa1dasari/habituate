# Node
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/.turbo/

# Java / Maven
api/target/
api/.mvn/wrapper/*.jar
api/.mvn/wrapper/maven-wrapper.properties

# Docker
*.log

# Editor
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Firebase — service account key is a secret, never commit it
*service-account*.json
*firebase-adminsdk*.json
*adminsdk*.json

# Mobile env (contains API keys)
mobile/.env.local
mobile/.env

# Firebase native config files (contain project identifiers — keep out of root)
google-services.json
GoogleService-Info.plist
